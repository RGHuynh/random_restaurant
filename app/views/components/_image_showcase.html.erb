<section class="showcase bg-light">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-6 order-lg-2 showcase-img map" id="googleMap">
      </div>
      <div class="col-lg-6 order-lg-1 my-auto showcase-text">
        <h1><%= @result["name"] %></h1>
        <h4><%= @result["price"] %></h4>
        <h4 class="description"><%= @result["rating"] %></h4>
      </div>
    </div>  
  </div>
</section>